# AdaptiveSurgeon backend — core (offline-runnable) dependencies
fastapi==0.137.1
uvicorn[standard]==0.49.0
SQLAlchemy==2.0.51
pydantic==2.13.4
pydantic-settings==2.14.1
python-multipart==0.0.32
numpy==2.4.6
opencv-python-headless==4.13.0.92

# ML (M4): trainable risk/phase models. Providers fall back to rules/heuristics
# if these are absent, so the default path still runs without them.
scikit-learn==1.9.0
joblib==1.5.3

# Testing
pytest==9.1.0
httpx==0.28.1

# --- OPTIONAL providers (NOT required; every provider has an offline fallback) ---
# Uncomment to enable real models. Platform remains fully runnable without these.
# ultralytics            # YoloDetector (InstrumentDetectionProvider)
# onnxruntime            # ONNX inference
# segment-anything       # SamSegmenter (AnatomySegmentationProvider)
# sentence-transformers  # SentenceTransformerEmbedder (EmbeddingProvider)
# psycopg2-binary        # PostgreSQL driver (DATABASE_URL=postgresql://...)
# boto3                  # S3/MinIO object storage (ADAPTIVE_STORAGE_BACKEND=s3)
